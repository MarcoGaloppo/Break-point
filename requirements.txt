# tennis_clt.py itself needs only numpy.
numpy>=1.24

# Additionally required to run clt_breakdown_tennis.ipynb.
pandas>=2.0
matplotlib>=3.7
jinja2>=3.1.2          # pandas Styler backend for the tables
jupyter>=1.0
